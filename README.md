# webDS
Web-based Nintendo DS emulator (WASM) scaffolding and frontend. Does NOT include BIOS or commercial ROMs. Build melonDS to WebAssembly using Emscripten and drop the generated artifacts into ./static/emu/ to run.
